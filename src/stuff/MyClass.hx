package stuff;

//needs to be in a file with the same name
class MyClass implements IMyInterface {
    public var MyField : String;

    public function new() {
        trace("A new MyClass instance was created!");
    }
}

